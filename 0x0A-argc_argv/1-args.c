#include <stdlib.h>
#include <stdio.h>

/**
 * main - programme that prints number of arguments passed
 *
 * @argc: number of commands
 * @argv: array of values
 *
 * Return: Always 0 (Success)
*/

int main(int argc, char __attribute__((unused)) *argv[])
{
	int i = 0, n;

	while (i < argc)
	{
		n = i;
		i++;
	}
	printf("%d\n", m);
	return (0);
}
