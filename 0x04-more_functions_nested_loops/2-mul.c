
/**
 * mul - Multiplies two intagers
 *
 * @a: represents the first intager digit
 * @b: represents the second intager
 *
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	result = a * b;

	return (result);
}
